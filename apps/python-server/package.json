{
  "name": "@frontend-labs/python-server",
  "private": true,
  "scripts": {
    "dev": ".venv/bin/uvicorn main:app --app-dir src --reload --port 8000",
    "install:py": ".venv/bin/pip install -r requirements.txt"
  }
}